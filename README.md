# 🤖 AI Vue 3 Coding Agent

Автономный ИИ-агент, построенный на **LangGraph** и **Google Gemini 2.5 Flash**.
Он умеет самостоятельно планировать задачи, писать код на Vue 3 (Composition API), редактировать существующие файлы, работать с роутером и исправлять свои ошибки.
---

## 🚀 Возможности

- **🧠 Двухфазная архитектура:**
  - **Planner (Планировщик):** Анализирует структуру проекта и составляет пошаговый план (создать, прочитать, изменить).
  - **Executor (Исполнитель):** Выполняет шаги, пишет код и запускает команды терминала.
- **❤️ Самоисцеление (Self-Healing):** Если команда `npm` упадет или файл не найдется, агент сам перестроить план и попробует исправить ошибку (до 3-х попыток).
- **📝 Контекстная работа:** Умеет читать файлы (`read`), чтобы сохранять стиль вашего кода при редактировании.
- **⚡️ Markdown-Driven:** Промпты (инструкции) и Задачи вынесены в отдельные `.md` файлы для удобного редактирования.

---

## 🛠 Стек технологий

- **LangGraph:** Управление состоянием агента и цикличной логикой.
- **Google Gemini 2.5 Flash:** Быстрая и дешевая модель с большим контекстным окном.
- **Node.js & TypeScript:** Среда выполнения.
- **Zod:** Валидация JSON-ответов от нейросети.

---

## 📂 Структура проекта

```text
/
├── prompts/              # 🧠 "Личность" агента (System Prompts)
│   ├── planner.md        # Инструкция для Планировщика
│   └── executor.md       # Инструкция для Исполнителя (Coding rules)
├── src/
│   ├── agent/            # Логика графа
│   │   ├── nodes/        # Узлы (PlannerNode, ExecutorNode)
│   │   ├── utils/        # Загрузчик промптов
│   │   ├── state.ts      # Состояние (память, ошибки, счетчик ретраев)
│   │   └── index.ts      # Сборка графа
│   └── run.ts            # Точка входа
├── task.md               # 📝 ВАША ТЕКУЩАЯ ЗАДАЧА
└── .env                  # API ключи
