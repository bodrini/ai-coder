# 🤖 AI Agent Engine: Universal Coding Partner

Автономный ИИ-движок на базе **LangGraph** и **Google Gemini**, трансформированный из узкоспециализированного скрипта в универсальную платформу. Агент адаптируется к любому стеку через конфигурацию, самостоятельно планирует задачи, пишет код и исправляет ошибки в режиме реального времени.

---

## 🚀 Ключевые возможности

* **🌍 Универсальная архитектура**: Движок больше не привязан к Vue 3. Благодаря `agent.config.json` агент динамически меняет свою `role`, `techStack` и `linterCommand` под нужды конкретного репозитория.
* **🤝 Human-in-the-Loop (Подтверждение плана)**: Интерактивный режим работы. Агент анализирует задачу, контекст проекта и выводит пошаговый план в консоль, ожидая одобрения пользователя перед внесением изменений.
* **❤️ Самоисцеление (Self-Healing)**: Встроенный цикл автономной валидации. Если после внесения правок линтер обнаруживает ошибки, агент автоматически перезапускает цикл планирования для их устранения (до 3-х попыток).
* **📂 Умный RAG (Контекстная память)**: Система индексации кода учитывает только разрешенные в конфигурации директории, что повышает точность работы и снижает расход токенов.
* **🧠 История и Память**: Все выполненные задачи фиксируются в локальном файле `.agent/history.md` внутри целевого проекта, формируя базу знаний для будущих итераций.

---

## 🛠 Стек технологий
* LangGraph: Оркестрация состояний и управление цикличной логикой графа.

* Gemini 2.5 Flash: Высокопроизводительная модель с контекстным окном до 1 млн токенов.

* TypeScript: Типобезопасная база кода агента.

* Vector RAG: Локальная база векторов для быстрого поиска релевантных фрагментов кода.

## ⚙️ Конфигурация проекта

Для настройки агента под конкретный проект создайте файл `agent.config.json` в его корне:

```json
{
  "projectType": "",
  "role": "",
  "linterCommand": "",
  "contextFiles": [""],
  "techStack": [],
  "rules": []
}
